import 'package:flutter/material.dart';

class Slider extends StatelessWidget {
  Slider(this.title, this.description, this.image);
  final String title;
  final String description;
  final String image;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
